import chalk from 'chalk';    //allows to define the color your are using to display a message

console.log(chalk.green('starting the app in dev mode,code by matynjr...')); //eslint-disable-line no-console
